#include "main.h"

/**
 * _isupper - Checks if a letter is uppercase
 * @x: The number to be checked
 * Returned: 1 for upper letter or 0 for any else
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 98)
	{
		return (1);
	}
	else
	return (0);
}
